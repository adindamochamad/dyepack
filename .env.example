# OpenAI API key for /api/agent live mode (optional — split demo uses scripted mode by default)
OPENAI_API_KEY=

# Vercel: vercel env add OPENAI_API_KEY production
# Check: curl https://dyepack-liard.vercel.app/api/health → liveLLM: true
